  Next steps (when the dry-run completes):
  - Verify achievements populated in DB
  - Switch LLM_ENRICHMENT_ENABLED=true + LLM_ENRICHMENT_MODE=sample → ~$0.05 test
  - Then live mode for 500 achievements → ~$1.50
  - Then full run → ~$10-15