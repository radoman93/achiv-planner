# Root-level env: variables interpolated by docker-compose (postgres image,
# flower basic auth). Service-level runtime config lives in backend/.env.
POSTGRES_USER=wow
POSTGRES_PASSWORD=change-me
POSTGRES_DB=wow_optimizer
FLOWER_USER=admin
FLOWER_PASSWORD=change-me
